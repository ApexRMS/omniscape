<?xml version="1.0" encoding="utf-8" ?>
<update comment="Update to omniscape 2.5 that migrates Julia configuration">

    <!-- Migrate Julia configuration from omniscape_juliaConfiguration to core_JlConfig -->
    <action code="Exec" condition="TableExists" criteria="omniscape_juliaConfiguration">
        <item>INSERT INTO core_JlConfig (ExePath) SELECT juliaPath FROM omniscape_juliaConfiguration</item>
        <item>DROP TABLE omniscape_juliaConfiguration</item>
    </action>

</update>
